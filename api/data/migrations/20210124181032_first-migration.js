exports.up = async (knex) => {
  await knex.schema
    .createTable('users', (users) => {
      users.increments('user_id')
      users.string('first_name', 200).notNullable()
      users.string('last_name', 200).notNullable()
      users.string('email', 200).notNullable()
      users.string('password', 200).notNullable()
      users.timestamps(false, true)
    })
    .createTable('events', (event) => {
      event.increments('event_id')
      event.integer('organizer', 200)
        .notNullable()
        .unsigned()
        .references('user_id')
        .inTable('users')
        .onDelete('RESTRICT')
        .onUpdate('RESTRICT')
      event.string('event_title', 200).notNullable()
      event.string('event_location', 200).notNullable()
      event.string('event_description', 200)
      event.date('event_date', 200)
      event.timestamps(false, true)
    })
    .createTable('dishes', (dish) => {
      dish.increments('dish_id')
      dish.string('dish_name', 200).notNullable()
    })
    .createTable('guests', (guest) => {
      guest.increments('guest_id')
      guest.integer('user_id', 200)
        .notNullable()
        .unsigned()
        .references('user_id')
        .inTable('users')
        .onDelete('RESTRICT')
        .onUpdate('RESTRICT')
      guest.integer('event_id', 200)
        .notNullable()
        .unsigned()
        .references('event_id')
        .inTable('events')
        .onDelete('RESTRICT')
        .onUpdate('RESTRICT')
      guest.string('dish', 200).notNullable()
      guest.timestamps(false, true)
    })
    .createTable('event_dishes', (event_dishes) => {
      event_dishes.increments('ed_id')
      event_dishes.integer('event_id', 200)
        .notNullable()
        .unsigned()
        .references('event_id')
        .inTable('events')
        .onDelete('RESTRICT')
        .onUpdate('RESTRICT')
      event_dishes.integer('dish_id', 200)
        .notNullable()
        .unsigned()
        .references('dish_id')
        .inTable('dishes')
        .onDelete('RESTRICT')
        .onUpdate('RESTRICT')
      event_dishes.integer('user_id', 200)
        .notNullable()
        .unsigned()
        .references('user_id')
        .inTable('users')
        .onDelete('RESTRICT')
        .onUpdate('RESTRICT')
    })
}

exports.down = async (knex) => {
  await knex.schema
    .dropTableIfExists('event_dishes')
    .dropTableIfExists('guests')
    .dropTableIfExists('dishes')
    .dropTableIfExists('events')
    .dropTableIfExists('users')
}
